name: Deploy to Vercel & Push to Docker Hub

on:
  push:
    branches:
      - main

jobs:
  deploy_to_vercel:
    runs-on: ubuntu-latest
    steps:
      - name: 🔄 Checkout del repositorio
        uses: actions/checkout@v4

      - name: 🚀 Instalar Vercel CLI
        run: npm install -g vercel

      - name: 🌍 Desplegar en Vercel
        run: vercel --prod --yes --token ${{ secrets.VERCEL_TOKEN }}

  push_to_docker_hub:
    runs-on: ubuntu-latest
    needs: deploy_to_vercel
    steps:
      - name: 🔄 Checkout del repositorio
        uses: actions/checkout@v4

      - name: 🔨 Construir imagen Docker
        run: docker build -t ${{ vars.DOCKER_USER }}/BongoCat-ContinuousDeployment:latest .

      - name: 🔑 Login en Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKER_USER }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: 📤 Subir imagen a Docker Hub
        run: docker push ${{ vars.DOCKER_USER }}/BongoCat-ContinuousDeployment:latest
